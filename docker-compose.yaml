name: distributed-transaction

services:
  mysql:
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=1234
    image: mysql
    volumes:
      - ./mysql_data:/var/lib/mysql

  redis:
    image: redis
    ports:
      - "6379:6379"
